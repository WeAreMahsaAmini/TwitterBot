{
    "name": "we-are-mahsa-amini-worker",
    "scripts": {
        "start": "NODE_ENV=production node dist/worker.js",
        "dev": "cross-env NODE_ENV=development nodemon",
        "build": "swc src -d dist --source-maps --copy-files"
    },
    "devDependencies": {
        "@types/node": "^18.8.0",
        "ts-node": "^10.9.1",
        "typescript": "^4.8.4"
    },
    "dependencies": {
        "@types/cron": "^2.0.0",
        "cron": "^2.1.0",
        "dotenv": "^16.0.3"
    }
}
